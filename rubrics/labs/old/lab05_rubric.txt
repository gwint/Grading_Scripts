Division (3 points)
	- 0.5 initialized list to new empty ArrayList properly

	-constructor
		-0.25 throws IllegalArgumentException if array is null
		-0.25 throws IllegalArgumentException if array is empty
		-0.5 copies values to list using add() method properly

	-removeZeroes()
		-ideally using Iterators
		-0.25 if not just make sure their functionality is correct
		-0.25 does not skip zeroes right next to eachother in the list

	-divide()
		-0.25 throws IllegalArgumentException correctly
		-0.25 uses the set method to alter list properly

	-0.5 toString() method is present and correct

FunWithStrings (3 points)
	-swapMaxes()
		-0.5 throws exception if either array is null
		-0.5 throws exception if either array is empty
		-0.5 throws exception if either array contains all null Strings
		-0.5 checks and ignores null Strings while finding the max
		-0.5 extracts max strings and their corresponding indices properly
		-0.5 properly swaps the maxes using set()

Zipper (2 points)
	-0.25 throws exception if either array is null
	-0.25 throws exception if arrays are different lengths
	-1 zips arrays together properly otherwise

Tester (worth 2 pts)
	-passes all test cases provided